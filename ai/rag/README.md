# RAG legado

O RAG operacional vigente está em `ai/operacao/rag/` e é consultado por `node scripts/agent-memory-rag.mjs search "<termos>"`.

Este diretório contém notas históricas, não um índice de recuperação. Não o trate como fonte autoritativa.
