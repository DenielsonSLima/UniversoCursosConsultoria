# Memória de agentes

A memória operacional versionada fica em `ai/operacao/`, para estar próxima das pastas históricas `ai/` e ser consumida pelo RAG local.

As decisões arquiteturais continuam em `docs/decisions/`. Este diretório fica como ponto de descoberta para documentação externa que precise ser adicionada ao manifesto RAG no futuro.
